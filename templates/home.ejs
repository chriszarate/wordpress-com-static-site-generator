<%- include( 'header', { title: 'Home', config } ); %>
<%- include( 'post-content', { post: posts[0] } ); %>
<%- include( 'footer', { about: true, archive: true, config, postId: posts[0].id, posts } ); %>
