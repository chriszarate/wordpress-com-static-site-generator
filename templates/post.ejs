<%- include( 'header', { title: post.title.rendered, config } ); %>
<%- include( 'post-content', { post } ); %>
<%- include( 'footer', { about: true, archive: true, config, postId: post.id, posts } ); %>
